name: inferno
description: The model-runtime boundary for on-device Golem inference.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  code_assets: ^1.2.1
  crypto: ^3.0.7
  ffi: ^2.2.0
  # 2.1.0 requires meta 1.19; Flutter 3.44.9 pinned 1.18 (#38), and 3.47.1's
  # 1.19 lifts that — held until the next reviewed `pub upgrade`.
  hooks: 2.0.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
